{
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "build": "turbo run build",
        "dev": "dotenv -- turbo dev"
    },
    "devDependencies": {
        "turbo": "^1.7.1"
    },
    "dependencies": {
        "dotenv-cli": "^7.0.0"
    }
}
