{
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "build": "turbo run build",
        "dev": "dotenv -- turbo dev"
    },
    "devDependencies": {
        "dotenv-cli": "^7.0.0",
        "turbo": "^1.7.1"
    }
}
