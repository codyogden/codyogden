{
    "name": "codyogden",
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "build": "turbo run build",
        "dev": "dotenv -- turbo run dev"
    },
    "devDependencies": {
        "turbo": "^1.9.8"
    },
    "dependencies": {
        "dotenv-cli": "^7.0.0"
    }
}
